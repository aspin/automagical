name: Rust

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Dependencies
      run: sudo apt-get install -y libasound2-dev
    - name: Build
      run: cargo build --no-default-features --features empty --verbose
    - name: Run tests
      run: cargo test --verbose
